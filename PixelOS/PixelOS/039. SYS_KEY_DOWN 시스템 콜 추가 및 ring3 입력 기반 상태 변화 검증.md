---
title: SYS_KEY_DOWN 시스템 콜 추가 및 ring3 입력 기반 상태 변화 검증
type: Review
created: 2026-04-01T20:14
updated: 2026-04-02T21:40
completed: true
tags:
  - Review
  - system-call
  - Ring3
  - keyboard
---
# 일일 목표

- [x] SYS_KEY_DOWN 시스템 콜 추가 ✅ 2026-04-01
- [x] ring3 입력 기반 상태 변화 검증 ✅ 2026-04-01

***
# 변경 사항

## 코드

**interrupts.c**

```C
...

// 시스템 콜 넘버 매핑용 Enum
enum
{
    SYS_DEBUG_PUTS = 1,
    SYS_GET_TICKS  = 2,
    SYS_KEY_DOWN   = 3,
};

// asm 코드에서 호출되는 시스템 콜 핸들러 정의
uint32_t syscall_handler(uint32_t syscall_no, uint32_t arg0, uint32_t arg1, uint32_t arg2)
{
    (void)arg1;
    (void)arg2;
	
    switch(syscall_no)
    {
        ...
		
        case SYS_KEY_DOWN:
        {
            // 키 눌림 여부 반환
            return keyboard_is_key_down((uint8_t)arg0) ? 1u : 0u;
        }
		
        default:
            return -1;
    }
```

`SYS_KEY_DOWN` 시스템 콜을 추가하였습니다. 시스템 콜 넘버는 `3`이며, **커널에서 특정 키 눌림 여부를 읽어 와 반환**하도록 작성하였습니다.


**kernel.c**

```C
...

__attribute__((section(".usertext"), noreturn))
void user_test_main(void)
{
    char test_msg_idle[] = "-";
    char test_msg_left[] = "<";
    char test_msg_right[] = ">";
	
    uint32_t last_phase = 0xFFFFFFFFu;
	
    for (;;)
    {
        // tick을 저장할 변수
        uint32_t ticks;
		
        // Left/Right 눌림 여부를 저장할 변수
        uint32_t left_down;
        uint32_t right_down;
		
        char* msg;
		
        // SYS_GET_TICKS
        __asm__ volatile (
            "int $0x80"
            : "=a"(ticks)
            : "a"(2), "c"(0), "d"(0), "b"(0)
            : "memory"
        );
		
         // SYS_KEY_DOWN (A: 0x1E)
        __asm__ volatile (
            "int $0x80"
            : "=a"(left_down)
            : "a"(3), "c"(0x1Eu), "d"(0), "b"(0)
            : "memory"
        );
		
        // SYS_KEY_DOWN (D: 0x20)
        __asm__ volatile (
            "int $0x80"
            : "=a"(right_down)
            : "a"(3), "c"(0x20u), "d"(0), "b"(0)
            : "memory"
        );
		
        if (left_down == right_down)
        {
            msg = test_msg_idle;
        }
        else if (left_down)
        {
            msg = test_msg_left;
        }
        else
        {
            msg = test_msg_right;
        }
		
        // 100Hz 기준 약 0.1초마다 페이즈 전환
        uint32_t phase = (ticks / 10u) & 1u;
		
        // 페이즈 변경 시
        if (phase != last_phase)
        {
            // 이전 페이즈 갱신
            last_phase = phase;
            
            // SYS_DEBUG_PUTS
            __asm__ volatile (
                "int $0x80"
                :
                : "a"(1), "c"((uint32_t)msg), "d"(0), "b"(0)
                : "memory"
            );
        }
    }
}

...

```

페이즈 갱신 주기를 `10 Tick`으로 줄이고, **`SYS_KEY_DOWN` 시스템 콜을 통해 `A, D` 키의 눌림 여부를 읽어 와 출력**하도록 유저 테스트 함수를 수정하였습니다.


***
# 테스트 결과

![[테스트 스크린샷-039-01.png]]
![[테스트 스크린샷-039-02.png]]
![[테스트 스크린샷-039-03.png]]

**왼쪽 키(`A`)를 누르면 `<`,
오른쪽 키(`D`)를 누르면 `>`,
아무 키도 누르지 않거나 양쪽 키를 모두 누르면 `-`가 출력**되는 것을 확인하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/03d2f3ef88018ad05f46dc784d57c8848489ff2f