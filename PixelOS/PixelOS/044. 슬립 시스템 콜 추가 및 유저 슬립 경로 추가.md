---
title: 슬립 시스템 콜 추가 및 유저 슬립 경로 추가
type: Review
created: 2026-04-03T22:23
updated: 2026-04-03T22:31
completed: true
tags:
  - Review
  - system-call
  - usermode
---
# 일일 목표

- [x] 슬립 시스템 콜 추가 ✅ 2026-04-03
- [x] 유저 모드 전용 슬립 경로 추가 ✅ 2026-04-03

***
# 변경 사항

## 코드

**syscall.h**

```C
...

// 앱/커널 공용 시스템 콜 번호 정의
typedef enum SyscallNum
{
    ...
    SYS_SLEEP      = 5,
} SyscallNum;
```

시스템 콜 번호 5번을 `SYS_SLEEP`으로 설정하였습니다.


**app/api.h**

```C
...

__attribute__((section(".usertext"), always_inline))
static inline void app_sleep(uint32_t ticks)
{
    __asm__ volatile (
        "int $0x80"
        :
        : "a"(SYS_SLEEP), "c"(ticks), "d"(0), "b"(0)
        : "memory"
    );
```

`SYS_SLEEP` 시스템 콜에 대한 API를 작성하였습니다.


**task.h**

```C
...

// 유저 모드 전용 슬립 함수
TrapFrame* task_sleep_from_user(TrapFrame* frame, uint32_t ticks);

...
```

**task.c**

```C
...

// 유저 모드 전용 슬립 함수
TrapFrame* task_sleep_from_user(TrapFrame* frame, uint32_t ticks)
{
    // 전달받은 Trap frame 포인터가 유효하지 않은 경우 무시
    if (!frame)
    {
        return frame;
    }
	
    // 현재 태스크 번호가 범위 밖인 경우 무시
    if (g_current < 0 || g_current >= g_count)
    {
        return frame;
    }
	
    // 0 tick 슬립은 단순 양보로 처리
    if (ticks == 0)
    {
        return task_yield_from_user(frame);
    }
	
    // 현재 태스크를 슬립 상태로 전환
    Task* t = &g_tasks[g_current];
	
    __asm__ volatile("cli");
    t->state = TASK_SLEEPING;
    t->wake_tick = g_timer_ticks + ticks;
    __asm__ volatile("sti");
	
    // 유저 모드 컨텍스트를 저장하고 다음 태스크로 전환
    return task_yield_from_user(frame);
}

...
```

유저 모드 전용 슬립 함수(`task_sleep_from_user`)를 구현하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/0f779b0551c19697adc4ae820addd7a3caa0fbf3