---
title: Pong 게임 오버 및 재시작 기능 추가
type: Review
created: 2026-05-02T15:28
updated: 2026-05-02T20:22
completed: true
tags:
  - Review
  - Pong
---
# 일일 목표

- [x] `10점`에 도달하면 게임이 오버되는 기능 추가 ✅ 2026-05-02
- [x] `R` 키를 통해 게임을 재시작할 수 있는 기능 추가 ✅ 2026-05-02

***
# 변경 사항

## 코드

**app/app_keys.h**

```C
...

// 게임 종료 시 게임 재시작
#define APP_KEY_RESTART     0x13
```

`R` 키를 통해 게임을 재시작할 수 있도록 제어하기 위해 스캔코드를 정의하였습니다.


**app/pong.h**

```C
// 게임 상태 관리를 위한 구조체
typedef struct PongState
{
    ...
	
    // 게임 오버 기준 점수
    int score_limit;
    // 게임 오버 여부
    int game_over;
    // 승자
    int winner;
	
    ...
    
    // 이전 프레임에 재시작 키(R)가 눌려 있었는지 확인하기 위한 멤버
    int restart_was_down;
} PongState;
```

게임 상태를 관리하는 구조체 `PongState`에 다음 멤버를 추가하였습니다.

- `score_limit` : 게임 오버의 기준이 되는 점수
- `game_over` : 게임 오버 상태인지에 대한 여부
- `winner` : 현재 게임의 승자
- `restart_was_down` : 이전 프레임에 재시작 키가 눌려 있었는지 확인하기 위한 멤버


**app/pong.c**

```C
...

#define SCORE_LIMIT 10

#define PLAYER_NONE 0
#define PLAYER_1 1
#define PLAYER_2 2

...
```

`score_limit` 멤버에 지정할 기준 점수인 10을 `SCORE_LIMIT`이라는 이름으로 정의하였습니다.

`winner` 멤버에 플레이어를 지정하기 위해 각 플레이어에 해당하는 넘버를 지정하였습니다.

- `NONE` : 0
- `PLAYER 1` : 1
- `PLAYER 2` : 2

```C
// pong 게임 초기화를 위한 내부 함수
__attribute__((section(".usertext")))
static void pong_init(PongState* game)
{
	...
	
	game->score_limit = SCORE_LIMIT;
    // 게임 진행 : 0, 게임 오버: 1
    game->game_over = 0;
    game->winner = PLAYER_NONE;
    
    ...
}
```

`pong_init` 함수에서 `score_limit`, `game_over`, `winner` 멤버를 초기화하였습니다.

```C
// pong 게임의 입력 관리를 위한 내부 함수
__attribute__((section(".usertext")))
static void pong_handle_input(PongState* game)
{
    int restart_down = app_key_down(APP_KEY_RESTART);
    
    // 재시작 키 푸시
    // R 키를 누르는 순간 한 번만 실행되도록 제어
    // 게임 종료 시 R 키를 누르면 게임 재시작
    if (game->game_over && restart_down && !game->restart_was_down)
    {
        pong_init(game);
    }
	
    game->restart_was_down = restart_down;
    
    ...
}
```

`pong_handle_input` 함수에서 게임 오버 상태에서의 `R` 키 입력을 통해 게임 재시작 로직이 작동하도록 구현하였습니다.

```C
// pong 게임 상태 갱신을 위한 내부 함수
__attribute__((section(".usertext")))
static void pong_update(PongState* game)
{
    // 게임이 일시정지 상태이거나 종료 상태인 경우 업데이트하지 않음
    if (game->game_over || game->paused)
    {
        return;
    }
    ...
    
    // 왼쪽 패들을 지나치는 경우: Player2 득점
    if (game->ball_curr_x + game->ball_size < 0)
    {
        if (game->player2_score < SCORE_LIMIT)
        {
            game->player2_score++;
            if (game->player2_score >= SCORE_LIMIT)
            {
                game->game_over = 1;
                game->winner = PLAYER_2;
            }
        }
        pong_reset_ball(game, 1);
    }

    // 오른쪽 패들을 지나치는 경우: Player1 득점
    if (game->ball_curr_x > game->game_w)
    {
        if (game->player1_score < SCORE_LIMIT) {
            game->player1_score++;
            if (game->player1_score >= SCORE_LIMIT)
            {
                game->game_over = 1;
                game->winner = PLAYER_1;
            }
        }
        pong_reset_ball(game, -1);
    }
	...    
}
```

`pong_update` 함수에서 게임 오버 상태인 경우 게임 상태를 업데이트하지 않도록 조건문을 수정하였습니다.

플레이어 득점 시 점수가 지정된 점수(`SCORE_LIMIT`)에 도달한 경우 `game_over` 멤버를 업데이트하여 게임 오버 상태로 진입하고, `winner` 멤버를 업데이트하여 승자를 저장하도록 구현하였습니다.

```C
__attribute__((section(".usertext")))
static void pong_score_to_text(int score, char* out)
{
    // 점수가 0 이하인 경우 0으로 고정
    if (score < 0)
    {
        score = 0;
    }
	
    // 점수가 99 이상인 경우 99로 고정
    if (score > 99)
    {
        score = 99;
    }
	
    // 점수가 10 이상인 경우 (10-99)
    if (score >= 10)
    {
        out[0] = (char)('0' + (score / 10));
        out[1] = (char)('0' + (score % 10));
        out[2] = '\0';
    }
    // 점수가 10 이하인 경우 (0-9)
    else
    {
        out[0] = (char)('0' + score);
        out[1] = '\0';
    }
}
```

정수형 점수를 문자열로 변환하는 내부 헬퍼인 `pong_score_to_text`에서 변환 가능한 범위를 99까지로 증가하였습니다. (경계값에 대한 유효성 검증을 위해 수정하였습니다.)

```C
__attribute__((section(".usertext")))
static void pong_render(PongState* game)
{
	...
	
	// 게임이 종료된 상태인 경우 Game Over 메시지와 승자 출력
    if (game->game_over)
    {
        char winner_text[] = "WINNER";
        char player1_win[] = "Player1";
        char player2_win[] = "Player2";

        int winner_text_w = 6 * 8;
        int winner_text_h = 16;
        int player_win_w = 7 * 8;
        int player_win_h = 16;

        int winner_text_x = (game->game_w - winner_text_w) / 2;
        int winner_text_y = (game->game_h - winner_text_h) / 2 - 8;
        int player_win_x = (game->game_w - player_win_w) / 2;
        int player_win_y = (game->game_h - player_win_h) / 2 + 8;

        // 텍스트 및 텍스트 배경 박스 출력
        app_game_fill_rect(
	        winner_text_x - 1, winner_text_y - 1,
	        winner_text_w + 2, winner_text_h + player_win_h + 4,
	        COLOR_BLACK
	    );
        app_game_draw_text(
	        winner_text_x, winner_text_y,
	        winner_text,
	        COLOR_YELLOW
	    );
        app_game_draw_text(
	        player_win_x, player_win_y,
	        (game->winner == PLAYER_1)
		        ? player1_win
		        : player2_win,
		    COLOR_YELLOW
		);
    }
	...
}
```

`pong_render` 함수에서 게임 오버 상태인 경우 승자에 대한 메시지를 출력하도록 로직을 작성하였습니다.


![[테스트 스크린샷-053-01.png]]

임의의 플레이어가 10점에 도달하면 게임 종료 상태가 되고, 메시지가 출력되는 것을 확인하였습니다.

![[테스트 스크린샷-053-02.png]]

게임 오버 상태에서 `R` 키를 눌러 게임을 재시작할 수 있는 것을 확인하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/46233fb6772b1c36cb5cb63821124814565eddcf