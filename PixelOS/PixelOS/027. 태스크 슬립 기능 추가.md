---
title: 태스크 슬립 기능 추가
type: Review
created: 2026-01-20T00:16
updated: 2026-02-23T14:18
completed: true
tags:
  - Review
  - task
  - scheduler
---
# 일일 목표

- [x] 태스크 슬립 기능 추가 ✅ 2026-01-20
- [x] 태스크 Runnable 상태 관리 ✅ 2026-01-20

***
# 변경 사항

## 코드

**task.h**

```C
// 인자가 없고 반환형이 void인 함수 포인터 타입의 별칭 정의
typedef void (*task_fn)(void*);

// 태스크 상태를 나타내는 enum 정의
typedef enum {
    TASK_RUNNABLE = 0,
    TASK_SLEEPING = 1,
} TaskState;

// Task 구조체 정의
typedef struct Task {
    ...
    
    TaskState state;        // 태스크 상태
    uint32_t wake_tick;     // 태스크를 Runnable 상태로 전환하는 tick
} Task;

...

void task_sleep(uint32_t ticks);

...
```

태스크에 **슬립 개념을 추가**하기 위하여 태스크 상태를 나타내는 열거형을 정의하였습니다. (`TASK_RUNNABLE`, `TASK_SLEEPING`)

`Task` 구조체에 현재 상태를 나타내는 `state`와, 슬립 상태에서 다시 실행 가능 상태로 전환될 기준 tick을 저장하는 `wake_tick` 멤버를 추가하였습니다.

현재 태스크를 지정한 tick 동안 실행 대상에서 제외시키는 함수 `task_sleep`를 선언하였습니다.


**task.c**

```C
...

// 태스크 테이블 초기화 함수
void task_init(void)
{
    for (int i = 0; i < MAX_TASKS; ++i) {
        ...
        g_tasks[i].arg = 0;
        g_tasks[i].alive = false;
    }
	...
}

...

// wake tick에 도달하였으면 태스크를 깨우는 함수
static void task_update_wakeup(Task* t)
{
    if (t->alive && t->state == TASK_SLEEPING)
    {
        if (g_timer_ticks >= t->wake_tick)
        {
            t->state = TASK_RUNNABLE;
        }
    }
}

// 다음에 실행할 alive 상태의 태스크 탐색 (Round-Robin 방식)
static int pick_next(int from)
{
    // 태스크 테이블이 비어 있는 경우 실패
    if (g_count == 0) { return -1; }
	
    for (int i = 1; i <= g_count; ++i)
    {
        int idx = (from + i) % g_count;
		
        Task* t = &g_tasks[idx];
        task_update_wakeup(t);
		
        if (t->alive && t->state == TASK_RUNNABLE)
        {
            return idx;
        }
    }
    return -1;
}

...

// 태스크 슬립 함수
void task_sleep(uint32_t ticks)
{
    if (ticks == 0)
    {
        task_yield();
        return;
    }
	
    if (g_current < 0 || g_current >= g_count)
    {
        return;
    }
	
    Task* t = &g_tasks[g_current];
	
    __asm__ volatile("cli");
    t->state = TASK_SLEEPING;
    t->wake_tick = g_timer_ticks + ticks;
    __asm__ volatile("sti");
	
    task_yield();
}

...
```

슬립 상태의 태스크를 일정 시간이 지나면 다시 실행할 수 있도록 `g_timer_ticks`와 `wake_tick`을 비교하여 태스크를 깨우는 내부 함수`task_update_wakeup`을 추가하였습니다.

`pick_next()`에서 각 후보 태스크에 대하여 `task_update_wakeup()`을 호출한 뒤, `alive`면서 `TASK_RUNNABLE`인 태스크만 선택하도록 **검사 로직을 추가**하였습니다.

`task_sleep(ticks)`은 **현재 태스크를 `TASK_SLEEPING` 상태로 전환**하고 **`wake_tick`을 설정**한 후, `task_yield()`를 통해 CPU를 양보하도록 구현하였습니다.


**kernel.c**

```C
...

// UI 작업을 위한 태스크
static void ui_task(void* arg)
{
    (void)arg;
	
    uint32_t last_tick = g_timer_ticks;
	
    for (;;) {
        // 프레임 경계 대기
        while (g_timer_ticks == last_tick) {
            task_sleep(1);
            continue;
        }
        last_tick = g_timer_ticks;
		
        ...
		
        compositor_compose();
        task_yield();
    }
}

// 앱 작업을 위한 태스크
static void app_task(void* arg)
{
    (void)arg;
	
    for (;;) {
        if (g_testwin) {
            window_put_char(g_testwin, '.', COLOR_RED);
        }
		
        task_sleep(1);
    }
}

...
```

UI 태스크(`ui_task`)의 프레임 경계를 기다리는 구간에서 기존의 **바쁜 대기(busy-wait)** 방식 대신 `task_sleep(1)`을 사용하여 **CPU 점유를 줄이도록 수정**하였습니다.

애플리케이션 태스크(`app_task`)에서 루프마다 `task_sleep(1)`을 호출하여 **실행 주기를 타이머 틱 단위로 제한**하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/be5cc8609e8dff3632d4d60d514dd618088659b6