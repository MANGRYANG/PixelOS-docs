---
title: SYS_GET_TICKS 시스템 콜 추가 및 시간 기반 ring3 동작 검증
type: Review
created: 2026-04-01T19:36
updated: 2026-04-02T21:39
completed: true
tags:
  - Review
  - system-call
  - timer
---
# 일일 목표

- [x] SYS_GET_TICKS 시스템 콜 추가 ✅ 2026-04-01
- [x] 시간 기반 ring3 동작 검증 테스트 ✅ 2026-04-01

***
# 변경 사항

## 코드

**interrupt.c**

```C
...

enum
{
    SYS_DEBUG_PUTS = 1,
    SYS_GET_TICKS  = 2,
};

// asm 코드에서 호출되는 시스템 콜 핸들러 정의
uint32_t syscall_handler(uint32_t syscall_no, uint32_t arg0, uint32_t arg1, uint32_t arg2)
{
    (void)arg1;
    (void)arg2;
	
    switch(syscall_no)
    {
        case SYS_DEBUG_PUTS:
        {
            const char* s = (const char*)arg0;
			
            // 기존 문자열 영역 지우기
            for (int i = 0; s[i] != '\0'; ++i)
            {
                remove_char(8 + i * 8, 8, COLOR_BLACK);
            }
			
            // 디버그 문자열 출력
            put_string(8, 8, s, COLOR_LIGHT_GREEN);
            gfx_present();
			
            return 0;
        }
		
        case SYS_GET_TICKS:
        {
            // tick 반환
            return g_timer_ticks;
        }
		
        default:
            return -1;
    }
}

...
```

`SYS_GET_TICKS` 시스템 콜을 추가하였습니다. 시스템 콜 넘버는 `2`이며, **커널에서 `Tick`을 읽어 와 반환**하도록 작성하였습니다.

문자열 출력 시스템 콜(`SYS_DEBUG_PUTS`)에서 **출력되는 문자열의 배경 색을 검정으로 변경**하도록 수정하여, 디버깅 문자열 겹침 현상을 방지하였습니다.


**kernel.c**

```C
...

__attribute__((section(".usertext"), noreturn))
void user_test_main(void)
{
    char test_msg_0[] = "tick phase A";
    char test_msg_1[] = "tick phase B";
    
    uint32_t last_phase = 0xFFFFFFFFu;
    
    for (;;)
    {
        // tick을 저장할 변수
        uint32_t ticks;
		
        // SYS_GET_TICKS
        __asm__ volatile (
            "int $0x80"
            : "=a"(ticks)
            : "a"(2), "c"(0), "d"(0), "b"(0)
            : "memory"
        );
		
        // 100Hz 기준 약 0.5초마다 페이즈 전환
        uint32_t phase = (ticks / 50u) & 1u;
		
        // 페이즈 변경 시
        if (phase != last_phase)
        {
            // 이전 페이즈 갱신
            last_phase = phase;
			
            // 디버그 메시지 변경
            char* msg = phase ? test_msg_0 : test_msg_1;
			
            // SYS_DEBUG_PUTS
            __asm__ volatile (
                "int $0x80"
                :
                : "a"(1), "c"((uint32_t)msg), "d"(0), "b"(0)
                : "memory"
            );
        }
    }
}

...
```

유저 모드 테스트 함수에서 `SYS_GET_TICKS` 시스템 콜을 통해 **`50 Tick`마다 문자열을 변경하며 출력**하도록 로직을 수정하였습니다.


***
# 테스트 결과

![[테스트 스크린샷-038-01.png]]
![[테스트 스크린샷-038-02.png]]

문자열이 **일정 시간 간격으로 변경되면서 출력**되는 것을 확인하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/073adb9e035b25ffa42cbbce4cc827f962442def