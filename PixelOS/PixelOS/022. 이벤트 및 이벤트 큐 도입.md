---
title: 이벤트 및 이벤트 큐 도입
type: Review
created: 2026-01-06T12:55
updated: 2026-02-23T14:18
completed: true
tags:
  - Review
  - event
---
# 일일 목표

- [x] 이벤트 도입을 통한 입력 처리 경로 정리 ✅ 2026-01-06

***
# 변경 사항

## 코드

**event.h**

```C
#pragma once
#include <stdint.h>

// 이벤트의 타입을 결정하는 enum 정의
typedef enum {
    EV_NONE = 0,
    EV_KEY,
    EV_MOUSE_MOVE,
    EV_MOUSE_BUTTON,
} EventType;

// Event 구조체
typedef struct {
    // Event 타입을 저장하는 멤버 변수
    EventType type;
    // Event 타입에 따라 key/mouse_move/mouse_button 중 정확히 하나만 유효
    // 나머지 접근 금지
    union {
        struct {
            // make 및 break에서 공통으로 사용하는 스캔코드(0x00~0x7F)
            uint8_t scancode;
            // 1 = pressed(make), 0 = released(break)
            uint8_t pressed;
            // pressed가 1인 상태에서만 유효(없는 경우 0)
            char ascii;
        } key;
			
        struct {
            // 업데이트 후 절대 좌표
            int x, y;
            // 패킷 기준 상대적 이동량
            int dx, dy;
        } mouse_move;
			
        struct {
            // MOUSE_LEFT/RIGHT/MIDDLE 중 하나
            uint8_t button;
            // 1 = pressed, 0 = released
            uint8_t pressed;
            // 현재 버튼 상태 비트마스크
            uint8_t buttons;
            // 이벤트 시점 커서의 절대 좌표
            int x, y;
        } mouse_button;
    };
} Event;
```

이벤트 기능 사용을 위한 **이벤트 구조체를 정의한 헤더 파일**입니다.

이벤트 구조체의 구조는 다음과 같습니다.

- 이벤트 타입을 지정하는 멤버 변수 (`type`)
- 이벤트 타입에 따라 **서로 다른 이벤트 데이터를 저장하는 공용체(union)**
	아래 세 구조체 중 정확히 하나만 유효하며, 나머지 멤버에 대한 접근은 금지됩니다.
	- 키보드 이벤트 (`key`)
	- 마우스 이동 이벤트 (`mouse_move`)
	- 마우스 버튼 이벤트 (`mouse_button`)


**event_queue.h**

```C
#pragma once
#include <stdbool.h>
#include "event.h"

void event_queue_init(void);

// IRQ에서 push 작업, 메인루프에서 pop 작업 수행
bool event_push(const Event* e);
bool event_pop(Event* out);
```

**이벤트 큐(Event Queue)**를 관리하는 인터페이스가 선언된 헤더 파일입니다.

**event_queue.c**

```C
#include "event_queue.h"

// 이벤트 큐 최대 크기
#define EVENT_Q_SIZE 128

// 이벤트 큐는 원형 큐 형태로 정의
static volatile int q_head = 0;
static volatile int q_tail = 0;
static Event q_buf[EVENT_Q_SIZE];

static inline void irq_disable(void) { __asm__ volatile("cli"); }
static inline void irq_enable(void)  { __asm__ volatile("sti"); }

// 이벤트 큐 초기화 함수
void event_queue_init(void)
{
    irq_disable();
    q_head = 0;
    q_tail = 0;
    irq_enable();
}

bool event_push(const Event* e)
{
    // 이벤트가 올바르지 않은 경우 push 실패
    if (!e)
    {
        return false;
    }
	
    // IRQ 비활성화
    irq_disable();
	
    int next = q_head + 1;
    if (next >= EVENT_Q_SIZE) next = 0;
	
    // 이벤트 큐가 가득 찬 경우
    if (next == q_tail)
    {
        // IRQ 활성화 후 push 실패
        irq_enable();
        return false;
    }
	
    q_buf[q_head] = *e;
    q_head = next;
	
    // IRQ 활성화 후 push 성공
    irq_enable();
    return true;
}

bool event_pop(Event* out)
{
    // 호출자가 제공한 출력 버퍼가 NULL 포인터인 케이스 방지
    if (!out)
    {
        return false;
    }
	
    // IRQ 비활성화
    irq_disable();
	
    // 이벤트 큐가 비어 있는 경우
    if (q_tail == q_head)
    {
        // IRQ 활성화 후 pop 실패
        irq_enable();
        return false;
    }
	
    // 큐의 tail 위치에 있는 이벤트를 꺼내어 출력 버퍼에 복사
    *out = q_buf[q_tail];
	
    int next = q_tail + 1;
    if (next >= EVENT_Q_SIZE) next = 0;
    q_tail = next;
	
    // IRQ 활성화 후 pop 성공
    irq_enable();
    return true;
}
```

이벤트 큐는 **원형 큐(Circular Queue) 구조**로 구현하였습니다.

이벤트 큐를 관리하기 위한 인터페이스는 다음과 같습니다.
- `event_queue_init` : 이벤트 큐 내부 초기화
- `event_push` : 이벤트 큐에 이벤트 추가
- `event_pop` : 이벤트 큐에서 이벤트 제거


**keyboard.c**

```C
...

// 인터럽트 핸들러에서 호출하는 함수
void keyboard_on_scancode(uint8_t scancode)
{
    ...
	
    // 이벤트 생성
    Event ev = {0};
    ev.type = EV_KEY;
    ev.key.scancode = code;
    ev.key.pressed = is_break ? 0 : 1;
    ev.key.ascii = 0;

    // break code인 경우 (키가 떼질 때 발생하는 신호인 경우)
    if (is_break)
    {
        // 키 떼짐
        key_state[code] = false;
		
        // Shift 키 신호인 경우
        if (code == 0x2A || code == 0x36)
        {
            // Shift 키의 상태를 false로 설정
            shift_pressed = false;
        }
		
        event_push(&ev);
        return;
    }

    // make code인 경우 (키가 눌릴 때 발생하는 신호인 경우)
    else {
        // 키 눌림
        key_state[code] = true;
		
        // Shift 키 신호인 경우
        if (code == 0x2A || code == 0x36)
        {
            // Shift 키의 상태를 true로 설정
            shift_pressed = true;
            event_push(&ev);
            return;
        }
		
        // Caps Lock 키 신호인 경우
        if (code == 0x3A)
        {
            // Caps Lock 상태를 변경
            capslock_enabled = !capslock_enabled;
            event_push(&ev);
            return;
        }
		
        // 실제 입력할 ASCII 문자로 변환
        char c = translate_scancode(code);
        
        // 버퍼 push
        if (c)
        {
            buffer_push(c);
            ev.key.ascii = c;
        }
        event_push(&ev);
    }
}
```

키보드 인터럽트가 발생할 때 실행되는 핸들러 함수에서, **키보드 입력 이벤트를 생성하고 해당 이벤트를 이벤트 큐에 삽입하도록 설정**하였습니다.


**mouse.c**

```C
...

// 3바이트 패킷이 완성되면 실행되는 함수
void mouse_on_packet(uint8_t p[3])
{
    ...
	
    // 마우스 이동 이벤트
    if (dx != 0 || dy != 0)
    {
        Event ev = {0};
        ev.type = EV_MOUSE_MOVE;
        ev.mouse_move.x = mouse_x;
        ev.mouse_move.y = mouse_y;
        ev.mouse_move.dx = dx;
        ev.mouse_move.dy = dy;
        event_push(&ev);
    }

    // 마우스 버튼 이벤트 (버튼 상태의 변화가 생긴 경우)
    uint8_t changed = (uint8_t)(prev_buttons ^ mouse_buttons);

    // 버튼 상태가 변한 경우
    if (changed)
    {
        const uint8_t buttons[3] = {
	        MOUSE_LEFT, 
			MOUSE_RIGHT,
			MOUSE_MIDDLE };
        for (int i = 0; i < 3; ++i)
        {
            uint8_t b = buttons[i];
            if (changed & b)
            {
                Event ev = {0};
                ev.type = EV_MOUSE_BUTTON;
                ev.mouse_button.button = b;
                ev.mouse_button.pressed = (mouse_buttons & b) ? 1 : 0;
                ev.mouse_button.buttons = mouse_buttons;
                ev.mouse_button.x = mouse_x;
                ev.mouse_button.y = mouse_y;
                event_push(&ev);
            }
        }
    }
    prev_buttons = mouse_buttons;
}

...
```

마우스 인터럽트에서 **3바이트 패킷이 완성되면 실행하는 함수(`mouse_on_packet`)**에서 패킷으로부터 계산한 이동량과 버튼 상태 변화를 검사한 뒤, **이동이 발생한 경우 `EV_MOUSE_MOVE` 이벤트를 생성**하고, **버튼 상태가 변한 경우 상태가 변경된 버튼마다 `EV_MOUSE_BUTTON` 이벤트를 생성**하여 이벤트 큐에 추가하도록 설정하였습니다.


**kernel.c**

```C
...

void kernel_main(void)
{   
    ...
	
    event_queue_init();
	
    asm volatile ("sti");
	
    // 윈도우 매니저 초기화
    wm_init();
	
	...
	
    while (1) {
        ...
		
        // 이벤트 처리
        Event ev;
        while (event_pop(&ev)) {
            switch (ev.type) {
            case EV_MOUSE_MOVE:
                cursor_set_pos(ev.mouse_move.x, ev.mouse_move.y);
                break;
				
            case EV_MOUSE_BUTTON:
				
                break;
				
            case EV_KEY:
                if (ev.key.pressed && ev.key.ascii) {
                    window_put_char(
	                    testwin,
	                    ev.key.ascii,
	                    COLOR_BLACK
	                );
                }
                break;
				
            default:
                break;
            }
        }
        
        compositor_compose();
    }
}
```

메인 커널 코드에서 이벤트 큐를 초기화하고, 매 틱마다 이벤트 큐를 확인하여 이벤트가 존재하면 해당 이벤트에 대한 작업을 수행하도록 작성하였습니다.

- 마우스 이동 이벤트(`EV_MOUSE_MOVE`)가 있는 경우, 커서 레이어의 좌표를 변경합니다.
- 마우스 버튼 이벤트(`EV_MOUSE_BUTTON`)의 경우는 아직 작성하지 않았습니다.
- 키보드 입력 이벤트(`EV_KEY`)가 있는 경우, 현재 윈도우에 입력 문자를 출력합니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/99154d1d334d97d26659cadbd7d4b9dd114ff4c5