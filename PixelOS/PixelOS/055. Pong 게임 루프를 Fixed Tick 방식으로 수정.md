---
title: Pong 게임 루프를 Fixed Tick 방식으로 수정
type: Review
created: 2026-05-02T21:05
updated: 2026-05-03T12:45
completed: true
tags:
  - Review
  - Pong
---
# 일일 목표

- [x] 고정 프레임 레이트 게임 루프 구현 ✅ 2026-05-02

***
# 변경 사항

## 코드

**app/pong.c**

```C
...

#define PONG_FRAME_TICKS 1u

...

__attribute__((section(".usertext"), noreturn))
void pong_main(void)
{
    // 게임 상태 관리를 위한 객체 선언
    PongState game;
  
    // pong 게임 초기화
    pong_init(&game);
  
    // 마지막으로 frame을 처리한 tick
    uint32_t last_frame_tick = app_get_ticks();
  
    // 게임 루프
    for (;;)
    {
        uint32_t now = app_get_ticks();
  
        // 아직 다음 frame을 처리할 tick이 되지 않았다면 CPU를 양보
        if ((uint32_t)(now - last_frame_tick) < PONG_FRAME_TICKS)
        {
            app_sleep(1);
            continue;
        }
  
        // 지연이 누적되었을 때 여러 frame을 몰아서 처리하지 않고,
        // 현재 tick 기준으로 다음 frame을 진행
        last_frame_tick = now;
  
        pong_handle_input(&game);
        pong_update(&game);
        pong_render(&game);
  
        app_sleep(1);
    }
}
```

기존의 단순 대기 방식에서 **Tick 기반의 고정 프레임 레이트(Fixed Frame Rate)** 제어 방식으로 로직을 개선하였습니다.

- `last_frame_tick`: 마지막으로 프레임을 처리한 시점의 시스템 틱(Tick) 값을 저장하는 변수입니다.
- `now`: 현재 루프의 시스템 틱을 실시간으로 확인하는 변수입니다.

프레임 처리가 완료된 후 `last_frame_tick = now;`으로 갱신함으로써, 만약 시스템 부하로 인해 특정 프레임이 지연되더라도 다음 프레임이 이를 억지로 따라잡지 않고 현재 Tick을 기준으로 다음 프레임을 진행하도록 구현하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/bb987a15399e9cf51344f66613013374257e4ecb

***

# 참조 문헌 및 링크

 - 게임 루프 관련 문서
	 https://dewitters.com/dewitters-gameloop/