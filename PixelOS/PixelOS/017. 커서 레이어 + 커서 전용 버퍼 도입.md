---
title: 커서 레이어 + 커서 전용 버퍼 도입
type: Review
created: 2025-12-13T06:39
updated: 2026-03-01T13:34
completed: true
tags:
  - Review
  - double-buffering
  - cursor
  - layer
---
# 일일 목표

- [x] 레이어 기능 도입 ✅ 2025-12-13
- [x] 커서에 대한 레이어 기능 설정 ✅ 2025-12-13
- [x] 커서 전용 버퍼 도입 ✅ 2025-12-13

***
# 변경 사항

## 코드

**layer.h**

```C
#pragma once

#include <stdint.h>

typedef enum {
    LAYER_DESKTOP,
    LAYER_WINDOW,
    LAYER_CURSOR,
} LayerType;

// 레이어 구조체 정의
typedef struct Layer {
    int x, y;
    int w, h;
    uint8_t* buffer;
    int z;
    LayerType type;
    int visible;
} Layer;
```

레이어 기능 도입을 위한 **레이어 관리 헤더 파일**입니다. 레이어 구조체를 정의하였습니다.

레이어 구조체가 가지는 정보는 다음과 같습니다.

- 화면에서의 좌표 (`x`, `y`)
- 레이어의 크기 (`w`, `h`)
- 백 버퍼 (`buffer`)
- z-index (`z`)
- 레이어 타입 (`type`)
- 가시성 (`visible`)


**cursor.h**

```C
...

// 커서 레이어 반환 함수
Layer* cursor_get_layer(void);

...
```

**기존에 커서의 정보를 저장하던 구조체를 제거**하였습니다. (레이어로 권한 이전)

커서 레이어를 반환하는 함수 `cursor_get_layer()` 함수를 추가로 선언하였습니다.


**cursor.c**

```C
...

// 커서 전용 버퍼
static uint8_t cursor_buffer[CURSOR_W * CURSOR_H];
// 커서 레이어 선언
static Layer cursor_layer;

void cursor_init(int x, int y)
{
    for (int i = 0; i < CURSOR_W * CURSOR_H; ++i)
    {
        cursor_buffer[i] = CURSOR_COLOR;
    }
    cursor_layer.x = x;
    cursor_layer.y = y;
    cursor_layer.w = CURSOR_W;
    cursor_layer.h = CURSOR_H;
    cursor_layer.buffer = cursor_buffer;
    cursor_layer.z = 100;   // 항상 최상위 레이어로 사용
    cursor_layer.type = LAYER_CURSOR;
    cursor_layer.visible = 1;
}

// 커서 레이어 반환 함수
Layer* cursor_get_layer(void)
{
    return &cursor_layer;
}

// 커서 위치 설정 함수
void cursor_set_pos(int x, int y)
{
    cursor_layer.x = x;
    cursor_layer.y = y;
}
```

커서 전용 백 버퍼(`cursor_buffer`)를 도입하였으며, 해당 백 버퍼를 멤버로 가지는 커서 레이어를 선언하였습니다.

**커서 초기화 함수**(`cursor_init`)에서 **커서 레이어 정보를 초기화**하도록 설정하고, **커서 위치 설정 함수**(`cursor_set_pos`)에서 **커서 레이어의 위치 정보를 조정**하도록 수정하였습니다.


**window.c**

```C
void wm_composite(void)
{
    ...
	
    // 커서 레이어
    Layer* cursor = cursor_get_layer();
    if (cursor && cursor->visible)
    {
        for (int y = 0; y < cursor->h; ++y)
        {
            int sy = cursor->y + y;
            if (sy < 0 || sy >= HEIGHT) continue;
			
            for (int x = 0; x < cursor->w; ++x)
            {
                int sx = cursor->x + x;
                if (sx < 0 || sx >= WIDTH) continue;
				
                uint8_t c = cursor->buffer[y * cursor->w + x];
                gfx_putpixel(sx, sy, c);
            }
        }
    }
}
```

`cursor_composite` 함수의 작동 로직을 **커서 레이어를 기반으로 작동**하도록 수정하였습니다.

기존 함수와 같이 백 버퍼에 직접 쓰는 방식도 고려하였으나, **레이어 합성 경로를 `gfx_putpixel()` 기반으로 통일**하여 출력 범위 검사를 그래픽스 엔진 API에서 수행하는 것이 **메모리 오염을 방지하고 렌더링 경로를 일관되게 관리**할 수 있어 **안전성** 측면에서 유리하다고 판단하였습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/f9589cdc48ca78d854be80ebe0f85e88ede6a5f5