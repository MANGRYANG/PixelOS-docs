---
title: Pong 중앙 기준선 및 텍스트 출력 기능 추가
type: Review
created: 2026-05-02T13:32
updated: 2026-05-02T15:54
completed: true
tags:
  - Review
  - Pong
---
# 일일 목표

- [x] 중앙 기준선 출력 ✅ 2026-05-02
- [x] 텍스트 출력 기능 추가 ✅ 2026-05-02

***
# 변경 사항

## 코드

**game_window.c**

```C
...

void kernel_game_draw_text(
	int x, int y, const char* text, uint8_t color
)
{
    if (!g_gamewin || !g_gamewin->in_use || !text)
    {
        return;
    }
	
    // 게임 렌더링 구역의 좌표로 변경
    const int origin_x = 1;
    const int origin_y = GAME_TITLE_HEIGHT + 1;
	
    const int clip_x0 = 1;
    const int clip_y0 = GAME_TITLE_HEIGHT + 1;
    const int clip_x1 = g_gamewin->width - 1;
    const int clip_y1 = g_gamewin->height - 1;
	
    int draw_x = origin_x + x;
    int draw_y = origin_y + y;
	
    if (draw_y < clip_y0 || draw_y + FONT_HEIGHT > clip_y1)
    {
        return;
    }
	
    // user pointer가 잘못되었을 때 무한히 읽지 않도록 10글자로 제한
    char buf[10];
    
    int i = 0;
    while (i < 10 && text[i] != '\0')
    {
        buf[i] = text[i];
        ++i;
    }
    buf[i] = '\0';
	
    window_draw_string(g_gamewin, draw_x, draw_y, buf, color);
}
```

게임 렌더링 구역의 특정 위치에 최대 10글자의 텍스트를 출력하는 함수를 구현하였습니다. (`kernel_game_draw_text`)


**syscall.h**

```C
#pragma once
#include <stdint.h>

// 앱/커널 공용 시스템 콜 번호 정의
typedef enum SyscallNum
{
    ...
    
    SYS_GAME_DRAW_TEXT  = 10
} SyscallNum;
```

텍스트를 렌더링하기 위해 시스템 콜 넘버를 정의하였습니다.

**interrupts.c**

```C
// asm 코드에서 호출되는 시스템 콜 핸들러 정의
TrapFrame* syscall_handler(TrapFrame* frame, uint32_t syscall_no, uint32_t arg0, uint32_t arg1, uint32_t arg2)
{
    switch(syscall_no)
    {
        ...
		
        case SYS_GAME_DRAW_TEXT:
        {
            uint16_t x = (uint16_t)(arg0 & 0xFFFF);
            uint16_t y = (uint16_t)((arg0 >> 16) & 0xFFFF);
			
            uint8_t color = (uint8_t)(arg1 & 0xFF);
            const char* text = (const char*)arg2;
			
            kernel_game_draw_text(x, y, text, color);
			
            frame->eax = 0;
            return frame;
        }
        ...
    }
    ...
}

```

`SYS_GAME_DRAW_TEXT` 시스템 콜을 통해 `kernel_game_draw_text` 함수를 호출하도록 **시스템 콜 핸들러를 작성**하였습니다.

**app/app_api.h**

```C
...

__attribute__((section(".usertext"), always_inline))
static inline void app_game_draw_text(uint16_t x, uint16_t y, const char* text, uint8_t color)
{
    uint32_t pos = ((uint32_t)y << 16) | x;
	
    __asm__ volatile (
        "int $0x80"
        :
        : "a"(SYS_GAME_DRAW_TEXT),
	      "c"(pos),
		  "d"((uint32_t)color),
	      "b"((uint32_t)text)
        : "memory"
    );
}
```

`app_game_draw_text` API를 추가하여, 해당 API를 통해 `SYS_GAME_DRAW_TEXT` 시스템 콜을 호출할 수 있도록 구현하였습니다.

**app/pong.c**

```C
...

#define CENTER_LINE_WIDTH 2
#define CENTER_LINE_HEIGHT 6
#define CENTER_LINE_GAP 4

...

__attribute__((section(".usertext")))
static void pong_render(PongState* game)
{
    // 게임 화면 초기화
    app_game_clear(COLOR_BLACK);
	
    // 중앙 선 렌더링
    int center_x = (game->game_w - CENTER_LINE_WIDTH) / 2;
    for (int i = 0; i < game->game_h;
	    i += (CENTER_LINE_HEIGHT + CENTER_LINE_GAP))
    {
        app_game_fill_rect(
	        center_x,
	        i,
	        CENTER_LINE_WIDTH,
	        CENTER_LINE_HEIGHT,
	        COLOR_LIGHT_GRAY
	    );
    }
    
    // 점수 UI 렌더링
    char score_player_1[] = "0";
    char score_player_2[] = "0";
	
    // 중앙선을 기준으로 대칭이 되어 출력하도록 설정
    app_game_draw_text(
	    game->game_w / 2 - 20, 4, score_player_1, COLOR_WHITE
	);
    app_game_draw_text(
	    game->game_w / 2 + 13, 4, score_player_2, COLOR_WHITE
	);
    
	...    
}

```

- 게임 윈도우 중앙에 **사각형을 주기적으로 배치하여 중앙선이 렌더링**되도록 구현하였습니다. 
- **중앙선을 기준으로 대칭이 되어 점수 텍스트가 출력**되도록 설정하였습니다.


![[테스트 스크린샷-050-01.png]]

중앙 기준선과 숫자가 올바르게 출력되었습니다.

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/fd0e7019991b56416d6cccadde04a44c5dad3af7

https://github.com/MANGRYANG/PixelOS/commit/a219ab3616b9290b9ff560f07d58a9fc596905cb