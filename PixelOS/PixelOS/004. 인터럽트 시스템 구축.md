---
title: 인터럽트 시스템 구축
type: Review
created: 2025-12-03T00:06
updated: 2026-02-23T14:18
completed: true
tags:
  - Review
  - kernel
  - interrupt
---
# 일일 목표

- [x] 인터럽트 시스템 구축 ✅ 2025-12-06

***
# 변경 사항

## 코드

**io.h**

```C
#pragma once

#include <stdint.h>

// 인자로 전달된 port에서 1바이트를 읽어 반환하는 함수 정의
static inline uint8_t inb(uint16_t port)
{
    // 반환값(포트에서 읽어 온 1바이트 값)을 저장할 변수
    uint8_t ret;
    
    // 내부 어셈블리 실행 및 삭제 금지 선언
    // ((port) -> DX) -> inb -> (AL -> ret)
    __asm__ volatile ("inb %1, %0" : "=a"(ret) : "dN"(port));
    
    return ret;
}

// 인자로 전달된 1바이트 value 값을 port에 전달하는 함수 정의
static inline void outb(uint16_t port, uint8_t value)
{
    // 내부 어셈블리 실행 및 삭제 금지 선언
    // ((value) -> AX) -> outb -> (DX -> port))
    __asm__ volatile ("outb %0, %1" : : "a"(value), "dN"(port));
}
```

**x86 CPU에서 I/O 포트에 직접 접근하기 위한 헤더 파일**입니다.

`inb(port)` 함수는 **포트 번호 `port`에서 1바이트를 읽어옵니다.**
`outb(port, value)` 함수는 **포트 번호 `port`에 1바이트(`value`)를 전달합니다.**



**idt.asm**

```Nasm
[BITS 32]

global idt_load
global timer_isr
global keyboard_isr

extern keyboard_handler
extern timer_handler

;---------------------------------------------------------------
; IDT 로드
;---------------------------------------------------------------
idt_load:
    mov eax, [esp+4]            ; 함수로 전달된 첫 번째 인자 (idtp 주소)
    lidt [eax]                  ; CPU에 IDT 정보 등록
    ret

;---------------------------------------------------------------
; 타이머 IRQ0 인터럽트 서비스 루틴
;---------------------------------------------------------------
timer_isr:
    pusha
    call timer_handler          ; timer_handler 호출
    popa

    mov al, 0x20                ; PIC EOI 명령 코드 전송
    out 0x20, al
    
    iretd                       ; 인터럽트에서 복귀

;---------------------------------------------------------------
; 키보드 IRQ1 인터럽트 서비스 루틴
;---------------------------------------------------------------
keyboard_isr:
    pusha
    call keyboard_handler       ; keyboard_handler 호출
    popa

    mov al, 0x20
    out 0x20, al                ; PIC EOI 명령 코드 전송

    iretd                       ; 인터럽트에서 복귀
```

**IDT(Interrupt Descriptor Table)를 로드하는 로직**과 **ISR(Interrupt Service Routine)**을 정의하는 어셈블리 파일입니다.

`idt_load`는 외부에서 호출 가능한 함수로, **첫 번째 인자로 `idtp` 주소를 전달받습니다.**
이후 `lidt` 명령어를 통해 **CPU의 `idtr` 레지스터에 IDT의 `base`와 `limit` 정보를 전달합니다.**

`timer_isr`은 **타이머 인터럽트(`IRQ0`)가 발생했을 때 CPU가 IDT를 통해 점프하는 ISR(Interrupt Service Routine) 엔트리 함수**입니다.
`timer_isr`은 다음과 같이 동작합니다.
1. 현재 레지스터 상태를 스택에 보관합니다.
2. **C 함수인 `timer_handler` 함수를 호출**합니다.
3. `timer_handler` 함수가 종료되면 레지스터 상태를 복구합니다.
4. **PIC(Programmable Interrupt Controller)**에 **EOI(End Of Interrupt)** 신호를 전달합니다.
5. `iretd` 명령을 통해 저장된 실행 상태를 복원하고 인터럽트 이전 코드로 복귀합니다.

`keyboard_isr`은 **키보드 인터럽트(`IRQ1`)가 발생했을 때 CPU가 IDT를 통해 점프하는 ISR(Interrupt Service Routine) 엔트리 함수**입니다.
`keyboard_isr`은 다음과 같이 동작합니다.
1. 현재 레지스터 상태를 스택에 보관합니다.
2. **C 함수인 `keyboard_handler` 함수를 호출**합니다.
3. `keyboard_handler` 함수가 종료되면 레지스터 상태를 복구합니다.
4. **PIC(Programmable Interrupt Controller)**에 **EOI(End Of Interrupt)** 신호를 전달합니다.
5. `iretd` 명령을 통해 저장된 실행 상태를 복원하고 인터럽트 이전 코드로 복귀합니다.

**interrupts.h**

```C
#pragma once

#include <stdint.h>
#include "../font/font.h"

void interrupts_init(void);     // IDT 및 PIC 설정
void timer_init(void);          // 타이머 IRQ 활성화
void keyboard_init(void);       // 키보드 IRQ 활성화
```

**인터럽트 시스템 관리를 위한 인터페이스를 정의한 헤더 파일**입니다.


**interrupts.c**

```C
#include "io.h"
#include "interrupts.h"

// IDT 엔트리 구조체
struct idt_entry
{
    uint16_t base_low;          // ISR의 하위 16 bit
    uint16_t selector;          // GDT 코드 세그먼트
    uint8_t  zero;              // 항상 0으로 설정
    uint8_t  type_attributes;   // Gate 타입, DPL, P 비트를 설정
    uint16_t base_high;         // ISR의 상위 16 bit
} __attribute__((packed));

struct idt_ptr
{
    uint16_t limit;             // IDT의 마지막 바이트 오프셋
    uint32_t base;              // IDT의 시작 주소
} __attribute__((packed));

#define IDT_SIZE 256            // IDT 엔트리 개수 = 256

static struct idt_entry idt[IDT_SIZE];
static struct idt_ptr idtp;

// idt.asm에서 제공되는 ASM 함수
extern void idt_load(uint32_t idtp_addr);
extern void timer_isr(void);
extern void keyboard_isr(void);

// IDT 초기화를 위한 간단한 memset 함수(내부 함수)
static void* memset(void *dest, uint8_t value, uint32_t size)
{
    uint8_t *p = (uint8_t*)dest;
    for (uint32_t i = 0; i < size; ++i)
    {
        p[i] = value;
    }
    return dest;
}

// IDT 엔트리 설정 함수
static void idt_set_gate(uint8_t idx, uint32_t base, uint16_t sel, uint8_t types)
{
    idt[idx].base_low  = (uint16_t)(base & 0xFFFF);
    idt[idx].selector  = sel;   // GDT 코드 세그먼트(0x08)
    idt[idx].zero = 0;
    idt[idx].type_attributes = types;
    idt[idx].base_high = (uint16_t)((base >> 16) & 0xFFFF);
}

// PIC 포트 매크로
#define PIC1_CMD  0x20
#define PIC1_DATA 0x21
#define PIC2_CMD  0xA0
#define PIC2_DATA 0xA1

// PIC 리매핑 함수(내부 함수)
// 마스터 PIC(IRQ 0~7) -> IDT 0x20~0x27
// 슬레이브 PIC(IRQ 8~15) -> IDT 0x28~0x2F
static void pic_remap(void)
{
    uint8_t pic1_mask, pic2_mask;

    // 현재 마스크 상태 저장
    pic1_mask = inb(PIC1_DATA);
    pic2_mask = inb(PIC2_DATA);

    // PIC에 ICW1(초기화 모드 진입 명령어) 전달
    outb(PIC1_CMD, 0x11);
    outb(PIC2_CMD, 0x11);

    // PIC에 ICW2(인터럽트 벡터 시작 오프셋) 전달
    outb(PIC1_DATA, 0x20);  // 마스터 PIC 오프셋: 0x20
    outb(PIC2_DATA, 0x28);  // 슬레이브 PIC 오프셋: 0x28

    // PIC에 ICW3(마스터-슬레이브 연결 구조) 전달
    // 마스터 PIC: 슬레이브 PIC는 IRQ2를 통해 연결 (2번째 비트가 1)
    outb(PIC1_DATA, 0x04);
    // 슬레이브 PIC: ID = 2
    outb(PIC2_DATA, 0x02);

    // PIC에 ICW4(PIC 추가 모드 정보) 전달
    outb(PIC1_DATA, 0x01);  // 8086 모드 사용
    outb(PIC2_DATA, 0x01);  // 8086 모드 사용

    // PIC에 OCW1(IRQ 마스킹 정보) 전달
    outb(PIC1_DATA, pic1_mask);
    outb(PIC2_DATA, pic2_mask);
}

// 타이머 tick을 저장할 변수 선언
volatile uint32_t g_timer_ticks = 0;

// asm 코드에서 호출되는 타이머 핸들러 정의
void timer_handler(void)
{
    // 타이머 tick 증가
    ++g_timer_ticks;
}

// 키보드 스캔코드를 저장할 변수 선언
volatile uint8_t g_latest_scancode = 0;

// 테스트 출력을 위한 16진수 변환 함수
static char hex_digit(uint8_t v)
{
    return "0123456789ABCDEF"[v & 0x0F];
}

// asm 코드에서 호출되는 키보드 핸들러 정의
void keyboard_handler(void)
{
    uint8_t scancode = inb(0x60);  // PS/2 스캔코드를 읽기
    g_latest_scancode = scancode;

    // 테스트용 스캔코드 출력 로직

    // 릴리즈 이벤트 무시
    if (scancode & 0x80)
    {
        return;
    }

    static int cursor_x = 0;    // X좌표
    static int cursor_y = 0;    // Y좌표

    char buf[3];
    buf[0] = hex_digit(scancode >> 4);  // 스캔코드 상위 4비트
    buf[1] = hex_digit(scancode);       // 스캔코드 하위 4비트
    buf[2] = 0;

    put_string(cursor_x, cursor_y, buf, 0x00);
    
    cursor_x += 16;  // 두 글자만큼 이동

    // 줄이 길어지면 다음 줄로 이동
    if (cursor_x >= 300)
    {
        cursor_x = 0;
        cursor_y += 16;
    }
}

// 인터럽트 초기화 함수
void interrupts_init(void)
{
    // IDT 포인터 구조체 초기화
    idtp.limit = (sizeof(struct idt_entry) * IDT_SIZE) - 1;
    idtp.base  = (uint32_t)&idt;

    // IDT를 0으로 초기화
    memset(&idt, 0, sizeof(idt));

    // PIC 리매핑
    pic_remap();

    // 타이머 인터럽트 설정(IRQ0) -> IDT 인덱스 0x20에 매핑됨
    // 타이머 인터럽트 발생 시 timer_isr 호출
    // 셀렉터(GDT 코드 세그먼트) 0x08
    // type_attributes 0x8E (DPL 0, 32비트 인터럽트 게이트)
    
    idt_set_gate(0x20, (uint32_t)timer_isr, 0x08, 0x8E);

    // 키보드 인터럽트 설정(IRQ1) -> IDT 인덱스 0x21에 매핑됨
    // 키보드 인터럽트 발생 시 keyboard_isr 호출
    // 셀렉터(GDT 코드 세그먼트) 0x08
    // type_attributes 0x8E (DPL 0, 32비트 인터럽트 게이트)
    idt_set_gate(0x21, (uint32_t)keyboard_isr, 0x08, 0x8E);

    // IDT 로드
    idt_load((uint32_t)&idtp);
}

// 타이머 활성화 (마스크 해제)
void timer_init(void)
{
    // 현재 마스킹 정보를 읽어 mask 변수에 저장
    uint8_t mask = inb(PIC1_DATA);

    // IRQ0 마스킹 해제
    mask &= ~(1 << 0);

    // 마스터 PIC에 마스킹 정보 적용
    outb(PIC1_DATA, mask);
}

// 키보드 활성화 (마스크 해제)
void keyboard_init(void)
{
    // 현재 마스킹 정보를 읽어 mask 변수에 저장
    uint8_t mask = inb(PIC1_DATA);

    // IRQ1 마스킹 해제
    mask &= ~(1 << 1);

    // 마스터 PIC에 마스킹 정보 적용
    outb(PIC1_DATA, mask);
}
```

**인터럽트 시스템 관리를 위한 C 코드**입니다.

`interrupts_init()` 함수의 작동 기제는 다음과 같습니다.
1.  CPU에게 IDT 정보를 전달하기 위한 `idt_ptr` 구조체 변수 `idtp`의 멤버를 초기화합니다.
2. IDT를 담는 테이블인 `idt`를 `memset` 함수를 사용해 초기화합니다.
3. `pic_remap()` 함수를 통해 **PIC가 CPU로 전달하는 인터럽트 번호를 리매핑**합니다.
4. `idt_set_gate()` 함수를 통해 **CPU가 PIC로부터 특정 인터럽트 번호를 수신하였을 때 어떤 ISR로 점프할지** 결정합니다. (IDT 엔트리 내용을 구성)
5. `idt.asm`의 `idt_load()` 함수를 통해  **CPU의 `idtr` 레지스터에 IDT의 `base`와 `limit` 정보를 전달**합니다.

`idt.asm`에서 호출되는 `timer_handler`와 `keyboard_handler`도 구현하였습니다.
	`timer_handler`에서는 **타이머 tick을 1 증가**시키는 작업을 수행합니다.
	`keyboard_handler`에서는 **PS/2 스캔코드를 읽어 16진수 문자열로 출력**합니다.


PIC에서는 **IMR(Interrupt Mask Register)**을 통해 인터럽트 마스킹을 수행하는데, 각 IRQ 라인의 인터럽트 마스킹을 해제하기 위해 `timer_init()` 함수와 `keyboard_init()` 함수를 구현하였습니다.
	`timer_init()` 함수는 **IRQ0에 대한 마스킹을 해제**합니다.
	`keyboard_init()` 함수는 **IRQ1에 대한 마스킹을 해제**합니다.


**kernel.c**

```C
...

    // 인터럽트 설정
    interrupts_init();
    timer_init();
    keyboard_init();
    asm volatile ("sti");
    
}
```

커널 코드에 **인터럽트 설정 함수를 추가**하고, **`sti` 명령을 통해 마스킹되지 않은 IRQ를 허용하도록 설정**하였습니다.


***
# 이슈 및 버그

## 증상

 - 키보드 인터럽트가 한 번 발생하면 그 이후로는 인터럽트에 반응하지 않음

## 재현

 - `idt.asm`에서 PIC에 EOI 신호를 전달하는 코드를 제거

## 원인(추정)

 - PIC가 EOI 신호를 전달받지 못해 인터럽트를 계속 '처리 중' 상태로 유지하는 것으로 추정됨

## 해결 및 우회 방법

 - ISR에서 IRQ 핸들링이 끝난 뒤 **PIC에 EOI 신호를 전달**하여, PIC가 해당 인터럽트 처리를 종료할 수 있도록 수정하여 해결하였음

***
# Commit Link

https://github.com/MANGRYANG/PixelOS/commit/9769055f8064aef209a89017686d9c575f56abfb

***

# 참조 문헌 및 링크

 - 입출력 엑세스를 위한 인라인 어셈블리 함수 관련 예제
	 https://wiki.osdev.org/Inline_Assembly/Examples#I/O_access

- IDT(Interrupt Descriptor Table) 관련 문서
	https://wiki.osdev.org/Interrupt_Descriptor_Table

- ISR(Interrupt Service Routine) 관련 문서
	https://wiki.osdev.org/Interrupt_Service_Routines

- Interrupt 관련 문서
	https://wiki.osdev.org/Interrupts
	https://gist.github.com/taekwon-dev/562238cac2d16b92ef43be1cf0260605

- PIC 관련 문서
	https://wiki.osdev.org/8259_PIC
	https://pdos.csail.mit.edu/6.828/2017/readings/hardware/8259A.pdf